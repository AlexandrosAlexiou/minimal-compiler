program test1
{
	declare a,k;

	{
		a := 100;
		k := a + 10;
		if(not [-3<5 or not [5>4]]) then 
			if(not [a<k or not [a>k]]) then 
			a:=1 
		else 
			a:=2;

		if( a > 1 and a < 2) then 
			a:= 3
		else 
			a := 4;

		k := 1000;

		while(a>3) 
			a := a-3;

		input(a);
		print(a);
		while(k>0)
			{print(10); 
			k := k-100}
	}
}

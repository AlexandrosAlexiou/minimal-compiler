program test2
{
	declare k;

	forcase
		when ( 1 > 2) : a := a + 3
		default: 
		{
			a := a + 4;
			a := a + 5;
			forcase
				when ( 6 > 7) : a := a + 8
				default: 
					a :=a + 9;
		incase
			when ( 10 > 20) : a := a + 30
			when ( 10 > 20) : a := a + 40
			when (not[3>4] or [not[3>4] and not[5>4]]) : a := a + 30;
		return a+3;
		call id();
		print ( (-3+4) * (-3+10) + (-3+4) * (-3+10)  );
		print ( id ()  );
		print ( 1000  )

		}
}